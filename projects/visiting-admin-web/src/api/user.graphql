# 查询示例
query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
  }
}

# 突变示例
mutation CreateUser($name: String!, $email: String!) {
  createUser(input: { name: $name, email: $email }) {
    user {
      id
      name
      email
    }
  }
}
